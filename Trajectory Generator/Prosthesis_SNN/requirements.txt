# Python >= 3.10
#
# Install PyTorch from the official selector for the target platform:
# https://pytorch.org/get-started/locally/
# Windows + NVIDIA should use a CUDA build when available.
# macOS arm64 should use the PyTorch build that supports CPU/MPS.

torch
snntorch>=0.9
numpy
PyYAML
