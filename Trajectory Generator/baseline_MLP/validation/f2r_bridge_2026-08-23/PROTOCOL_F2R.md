# F2R — Protocollo rev3 (preregistrato; sorgente machine-readable: `f2r_protocol.json`)

**Protocol id:** `F2R-S0-bridge-rev3` · **Data:** 2026-08-23 · **Stadio autorizzato:** S0 (tooling, test sintetici, dry-run) · **Origine:** `reports/user/2026-08-23_f1_s1_protocollo_tooling_dryrun.md` §13 (revisione 3, PASS dell'architetto). Ogni soglia, split, budget e sequenza qui riportati sono identici al JSON, che è l'unica fonte letta dal tooling.

## 1. Attori pinnati
- **Init primaria immutabile: JUL_H0 35D** (`rl_module_last` del warmup del 13/07), actor digest `a0801a9e…`, `module_state.pkl` `44457ca5…`; 10 chiavi `pi*`, colonne clock 0:2 zero, σ 0,005 costante.
- **Controllo negativo (mai init): B0820_H0** (`ce46f729…`).
- **Teacher privilegiato offline: V26_39D** (`5bbc6cbd…`) alimentato dai 4 target prescritti (2:6) e dal clock destro prescritto (0:2) = A_iso6clk; valutato offline sugli stati visitati dallo student (forward numpy float64, scarto ≤ 1e-5 dal percorso torch).
- **Anchor:** solo le 3 trace A_iso6clk esistenti (`05de8103…`, `3a66d109…`, `5375547a…`), 1 500 righe; nessun nuovo rollout del teacher prima dell'audit di tooling, P0 e T1.

## 2. Seed e split
- Det 123 = lato training (misura gate A/B/C e aggregazione se preregistrata); stocastici 123/124 = raccolta/aggregazione.
- **125 = gate held-out di promozione V**: mai in dataset, etichette, fit, aggregazione, selezione batch o tuning; solo go/no-go; un FAIL blocca la promozione a R/F3.
- 126–128 = test sigillato (F3, aperti una sola volta).

## 3. Semantica delle azioni (verificata nel codice)
Azione assoluta per giunto: `q_knee = 0,75·a − 0,75`, `q_ankle = 0,7·a` (bounds knee (−1,5, 0), ankle (−0,7, +0,7) rad), clip e slew per componente (2,5/2,0 rad/s vs endpoint precedente); teacher IK `u_IK(t) = encode(q_IK(t + 0,01 s))`, lookahead 0. Composizione per giunto ben posta a livello di comando, non equivalente comportamentalmente: T2 ha gate propri.

## 4. Varianti e budget
T1 commissioning (u_T, 1 round, 3 det, STOP sempre, fuori budget) · **T2 primaria** (knee u_T, ankle u_IK; R ≤ 4) · T3 contingenza (u_IK; R ≤ 2; solo se T2 passa A e fallisce B3 o diagnosi equivalente B2/B5; nessun tuning). Refit: Adam lr 1e-4, batch 256, 300 epoche, seed 2026, λ_clip 1,0, λ_φ 0,1, λ_a 1e-3, selezione solo closed-loop; testa ausiliaria solo training-time; export con le sole 10 chiavi originali e save/reload bit-exact.

## 4bis. T1R — commissioning correttivo (decisione dell'architetto, 23/08; tooling implementato, refit/rollout NON eseguiti)
Init = **JUL_H0** (mai lo student T1). **Task** = 3 anchor pinnati + stati on-policy dei 3 rollout det T1 (seed 123), etichette knee `u_T` / ankle `u_IK` (lookup esatto su `t_pre`); **preservazione** = stati dei 3 job det JUL_H0 (seed 123), target = azione JUL registrata (mai `u_T`). Loss normalizzata per giunto (varianze fissate a priori), medie per ruolo/giunto (group-balanced, maschere separate), **β = 1**, stesso budget congelato. Dedup entro ruolo (conflitti → fail-closed); le 3 collisioni cross-ruolo (righe di reset) sono rimosse dal task, preservazione prevale, riportate con digest dell'input e delta dei target; conteggi attesi 2 994 / 1 176 (fail-closed). Criteri offline obbligatori prima di export/rollout: P1 deviazione da JUL sugli stati JUL ≤ 0,10/giunto; P2 ankle vs `u_IK` ≤ 0,10 (anchor) e ≤ 0,15 (stati T1); P3 knee vs `u_T` ≤ 0,50 (anchor); **P3b** knee ≤ 0,75 (stati T1); P4 invarianti (colonne clock, log-std, 10 chiavi, save/reload, invarianza clock); P5 shift parametri informativo. Poi esattamente 3 rollout det seed 123 **gate-only** (`enters_dataset false`, `generates_labels false`, mai aggregati) → gate A (A6 unwrapped); B/C informativi; V/R non valutati; **STOP sempre**; `unlock_t2 = A.pass`; nessuna promozione, nessun trigger T3, nessuna aggregazione. T2 round 1 parte dallo student T1R **solo** dietro un artefatto di gate T1R verificato con A PASS (altrimenti bloccato, nessun fallback). I 75 job legacy restano invariati; +3 job T1R = 78. **Emendamento preflight-scoping (23/08):** il preflight globale del driver mantiene per ogni output esistente i controlli d'integrità archivistica (receipt, chiusura F0, status/rc, SHA, binding, `job_inputs`); l'uguaglianza con la revisione corrente del tooling (`f2r_driver_inputs_digest`, `git_head`) è richiesta solo per i job selezionati; per i non selezionati il mismatch è informativo ed esplicito. **Tentativi T1R:** orchestration attempt #1 (tag `20260823_185113`) abortito in preflight con zero rollout; il successivo lancio dopo audit è il primo e unico closed-loop rollout attempt.

## 5. P0 (offline; NON eseguito in S0)
Fold LOSO sui 3 start JUL_H0: train = gli altri due start JUL_H0 + i 3 anchor **meno esattamente una riga** (emendamento B, 23/08, pre-metrico: la riga di reset — indice 0, prima osservazione pre-azione con `t_pre == reset time` — dell'anchor dello stesso start held-out, bit-identica alla riga 0 del test); test = il solo start held-out, **completo** (500/500/176 righe, riga di reset inclusa); le altre 1 499 righe anchor e gli altri due start JUL restano nel train; validazione strutturale fail-closed (provenienza temporale, identità bit-exact, unicità, esattamente una collisione prima e zero dopo); standardizzazione solo sul train; etichetta c(t_pre) prescritta offline; vietati tempo/indice/`gait_phase_*`; variante pre-ciclo senza terna clock online. Stimatori: ridge λ 1e-2; MLP 64-64 tanh, Adam 1e-3, 200 epoche, seed 2026. PASS: mediana ≤ 0,05 e p90 ≤ 0,20 in ≥ 2 fold su 3 (MLP), fold +0,20 sempre riportato, calcolo unico.

## 6. Gate (soglie congelate; aggregazione per start, mediana sui cicli validi, start senza cicli = FAIL)
**A** A1 primo HS ∈ [0,15, 2,0] s; A2 ≥ 1 ciclo/start e ≥ 2 a ≥ 2 start; A3 HS cancellate 0, resync ≤ 1; A4 orizzonte 500 senza timeout; A5 clock online informativo ≤ 3,5 s; A6 periodo ∈ [1,45, 1,70] s, deriva ≤ 0,055 ciclo/s (**emendamento A6-unwrap, 23/08**: deriva = pendenza della differenza circolare prescritta−online **unwrapped** dopo l'onset del clock online, righe sature escluse; soglia invariata); A7 stance ≥ 0,30. **B** B1 knee ROM ≥ 0,60; B2 knee RMSE ≤ 0,16 e r ≥ 0,85 (2/3); B3 min caviglia in fase [0,55, 0,80] ≤ −0,03 (2/3); B4 ankle RMSE ≤ 0,25 (2/3); B5 stance ∈ [0,50, 0,75] (2/3); B6 settled ≥ 60 %, scartati ≤ 40 %. **C** C1 penetrazione ≤ 20 mm; C2 nessuna terminazione per penetrazione; C3 reserve ≤ 1 000 Nm per start; C4 clip ≤ 30; C5 |a| ≤ 1,3. **V** (125): orizzonte 3/3, ≥ 1 ciclo/start, nessuna penetrazione, C1–C3. **R**: 9 stocastici, orizzonte ≥ 8/9, cicli ≥ 1 in ≥ 8/9, C1–C3.

## 7. Sequenza rev3
S0 → audit → P0 → audit → T1 → audit → **T1R (commissioning correttivo) → audit → T2 solo se A PASS** → T2 round (D_r → refit → rollout 123 det / 123–124 stoch / 125 stoch → A → B → C → V → promozione a R senza aggregazione oppure aggregazione solo 123/124 → D_{r+1}) con audit per round → (T3) → R → audit → F3 sigillato e F4. Ogni FAIL arresta fail-closed.
