# V26B — Protocollo preregistrato **rev3** (2026-08-24; waiver di copertura RESPINTO dall'architetto; rev2 superseded, SHA `404bcb1e…`/`94b4dba8…`): V26 agosto 39D → student causale 35D → gate deterministico → sweep σ → critic warmup

**Stato: rev3 — V0 tooling implementato (`v26b_anchors.py`, SELFTEST PASS 256 check, incl. smoke di materializzazione dei moduli σ e lifecycle no-clobber su fallimento parziale; librerie F0/F1/F2R riusate in sola lettura, immutate — fix v0.1: la derivazione σ non pre-crea più l'out_dir del tool F1, e per σ 0,0025/0,01 usa un mirror fedele con entropia attesa calcolata da σ, bit-identico al tool F1 a 0,005) + dry-run no-clobber (manifest 112937 storico, pin pre-fix; nuovo dry-run obbligatorio prima del GO); raccolta anchor, V1–V5, rollout/fit/gate NON eseguiti e NON autorizzati. La raccolta reale richiede `--collect --authorized-stage V26B-ANCHORS` (GO dell'architetto).** Nessun tooling, training, rollout, gate, cache o refit è eseguito da questo documento; ogni stadio richiede il GO esplicito dell'architetto e si arresta fail-closed. Fonte machine-readable: `v26b_protocol.json` (unica letta dal futuro tooling); questo MD ne è lo specchio.

## Perché (evidenza)
La ricetta di luglio che produsse JUL_H0 (`a0801a9e…`) fu: warm start imitativo → trapianto actor-only feature-aligned → **adattamento target-domain supervisionato** con teacher IK su entrambi i giunti, ancore del source actor (~65 % del dataset), DAgger sugli stati visitati con **label phase-aligned** (troncamento al primo mismatch FSM), logstd congelato, contratto deployable (clock prescritto azzerato) → 3/3 start det + 3/3 seed σ 0,005 → critic warmup 1 iter (report 25/06, 09/07, 11/07, 13/07, 15/07). Il piano attuale la replica partendo dal **V26 di agosto (39D, `5bbc6cbd…`), unica init e unica fonte di conoscenza di rete**, sotto il runtime corrente (detector V26, FSM v3, corridoio morfologico event-anchored; config pinnata `a870cc38…`, **nessuna guardia modificata**: penalità/terminazione penetrazione 20/28 mm invariate). JUL_H0, T1 (`7f45a7ab…`), T1R (`da4937c1…`) e B0820 (`ce46f729…`) sono **soli controlli**: mai init, mai etichette, mai dataset.

## Audit dei contratti di feature (39D → 35D, per nome)
I due manifest pinnati (`2837779c…` 39D, `c6f86028…` 35D) condividono **35 nomi nello stesso ordine relativo**: il vettore 39D = vettore 35D con il blocco dei 4 target prescritti (`healthy_{knee,ankle}_angle_imitation_target[_vel]`) inserito agli indici **2:6** (verificato: 39∖35 = i 4 target; 35∖39 = ∅). Distinzione obbligatoria: (i) il **clock prescritto** `gait_phase_sin/cos` (indici 0:2 del 35D) **resta nel vettore** ma sotto v3 è morto (l'env emette le costanti (0, 1)); nello student le colonne W₁[:,0:2] sono **azzerate** a ogni passo e all'export, con invarianza bit-identica verificata → fisicamente presente, funzionalmente **non** un input; (ii) i **4 target prescritti** non sono azzerati: sono **assenti dal vettore 35D**. Sostituti causali online, per nome: il clock prescritto → terna clock online protesica `online_left_gait_phase_sin`, `online_left_gait_phase_cos`, `online_left_cycle_duration_s` (35D 14–16; (0,1,0) fino al secondo HS accettato, poi fase clip senza wrap) + feature FSM 17–24 (`phase_fsm_wait_hs/stance_after_hs/swing_after_to`, `phase_expected_hs/to`, `phase_stance/swing_elapsed_norm`, `phase_cycle_progress_credit`); i target prescritti → **nessun sostituto diretto**: la loro funzione (riferimento articolare indicizzato alla fase) va **internalizzata** dalla mean-network a partire da memoria del controller 25–34 (`*_previous_endpoint`, `*_served_ref[_vel/_accel]`, `*_sea_u`), FSM 17–24, clock online 14–16 e blocco contatto/GRF 10–13 — che è esattamente ciò che l'adattamento V2 addestra.

## Stadi (ognuno termina con STOP + audit)
- **V0 — Tooling** (stile S0): tooling + test sintetici + dry-run no-clobber; riuso read-only delle librerie F0/F1/F2R (cache privilegiate, labeller, gates con A6-unwrap, driver con preflight scoping, export transazionale). Vietati training/rollout/gate/refit.
- **V1 — Trapianto init 39→35** (ricetta 25/06 + 09/07 + regola deployable 13/07): copia di W₁ **per nome feature** (35 condivise dei manifest pinnati `2837779c…` → `c6f86028…`); le 4 colonne dei target sani (2:6 del 39D) rimosse **con compensazione mean-bias** (`b₁ += W₁[:,rimosse]·media(target sulle 3 trace anchor)`; il drop secco è sconsigliato dal report di giugno); colonne del clock prescritto (0:2 del 35D) **azzerate**; strati nascosti e testa media copiati; testa log-std sostituita con una testa costante a **σ_placeholder_di_serializzazione = 0,005** — un segnaposto richiesto dal formato del modulo, **irrilevante per il gate deterministico** (V3 valuta solo la media) e **non** un default né una scelta operativa implicita: il σ operativo è deciso esclusivamente dallo sweep V4; export 35D puro con l'intera batteria di invarianti. Nessun rollout in V1.
- **V2 — Adattamento target-domain** (ricetta 13/07): adattamento supervisionato dell'**intera mean-network**, logstd congelato. **ANCHOR COVERAGE (rev3, obbligatoria, NESSUN WAIVER).** Target vincolante: **≥ 16 000 righe bitwise-uniche** (ruolo dei 16 000 anchor di luglio) e **≥ 3 trace complete (500 righe) per ciascun σ**; sotto target → **STOP e decisione dell'architetto, nessun seed aggiunto automaticamente**. **Target = SEMPRE la mean action deterministica del V26 di agosto**, calcolata offline sugli obs39 visitati (route A_iso6clk; sulle trace deterministiche la mean deve coincidere con l'azione registrata ≤ 1e-5, verificato per trace); **mai** l'azione stocastica registrata. Comportamento di raccolta: moduli 39D a σ costante derivati da V26 (mean bit-identica) sulla **griglia fissa {0,0025, 0,005, 0,01}** — serve solo alla **copertura degli stati** e **non determina né orienta il σ operativo** (deciso in V4). **Seed anchor-only preregistrati {1000, 1001, 1002, 1003}**, 3 start esatti → **36 trace stocastiche × 500 attese + 1 500 righe det esistenti** (pin `05de8103…`/`3a66d109…`/`5375547a…`) = 19 500 grezze. Seed **123–128 esclusi e immutati** nei loro ruoli (123/124 DAgger, 125 held-out, 126–128 sigillati); i seed anchor sono vietati in ogni contesto DAgger/gate/holdout e viceversa (assert nel tooling). Filtri fail-closed: identità della trace (seed/selezione/start esatto/n_actor/steps; sidecar `aiso6clk` con `all_steps_asserted`, `steps_match_rollout`, inserzione legata ai manifest pinnati), digest (trace/summary/reset/sidecar; config `a870cc38…`; `rollout_eval` `5433bcbc…`; mean del modulo σ = mean V26, derivazione pinnata a `5bbc6cbd…`), nessun contract failure causale, righe finite, **dedup bitwise su tutte le righe** (duplicazione vietata), trace incomplete scartate e conteggiate.
  Ruoli del dataset: **(a) ancore V26** = l'insieme che ha superato l'ANCHOR COVERAGE GATE, target = azione V26 registrata/riprodotta su quegli stati (preservazione dell'init sulla sua regione di competenza); **(b) teacher IK ginocchio+caviglia** (`u_IK(t)=encode(q_IK(t+0,01 s))`, lookahead 0, lookup esatto su `t_pre`) sulle righe degli start alternativi e su ogni riga on-policy — **nessuna etichetta di rete fuori dalle ancore** (lezione F2R: il teacher 39D estrapola fuori supporto; l'IK è on-support per costruzione); **(c) DAgger phase-aligned fail-closed**: round sugli stati visitati dallo student (det 123, stoch 123/124), etichette `u_IK`, con la regola obbligatoria: le label valgono solo fino al **primo mismatch FSM discreto** fra rollout e ciclo prescritto; le righe successive sono **troncate** (mai rietichettate per indice); frazione troncata riportata per trace; un round con frazione fuori-fase pre-troncamento > 0,80 è respinto (diagnosi 13/07: 76,3 % di label disallineate distrussero l'adattamento). Loss normalizzata per giunto, medie per ruolo (group-balanced), **β = 1**, budget congelato (Adam 1e-4, batch 256, 300 epoche, seed 2026, λ_clip 1, λ_φ 0,1, λ_a 1e-3), testa ausiliaria di fase solo training. **DAgger ≤ 3 round** (luglio promosse r2 e respinse r3); aggregazione solo seed 123/124. Criteri offline fail-closed prima di ogni rollout: **Q1** preservazione sulle ancore ≤ 0,10/giunto; **Q2/Q2b** vs `u_IK` ≤ 0,15/giunto (start alternativi e righe on-policy); **Q3** invarianti (10 chiavi, clock azzerato, σ costante, save/reload bit-exact, invarianza clock); **Q4** reporting (troncamenti, conteggi, dedup e collisioni di reset con digest+delta, shift parametri informativo). Qualsiasi FAIL → nessun export, nessun rollout.
- **V3 — Gate deterministico** (start esatti **{−0,20, nominale, +0,20}** = `episode_start_offset_s` {1,756870983805102; 1,956870983805102; 2,156870983805102}, seed 123): **D1 orizzonte 500/500 su tutti e 3 gli start** (nessun timeout di fase); D2 ≥ 1 ciclo/start e ≥ 2 in ≥ 2; **D3 i quattro contatori v3 puliti su tutti gli start**: `hs_cancelled_count = 0`, `resync_count ≤ 1`, `phase_timeout_stance/swing = 0 righe`, `morphology_causal_contract_failure = 0` (`failed_closed false`); D4 cinematica vs corridoio (`33b1dd7c…`, α 0,6223): knee RMSE ≤ 0,16 e r ≥ 0,85 (2/3), **min caviglia in fase [0,55, 0,80] ≤ −0,03 (2/3)**, ankle RMSE ≤ 0,25 (2/3), stance ∈ [0,50, 0,75] (2/3), knee ROM ≥ 0,60, coverage settled ≥ 0,60 / dropped ≤ 0,40; D5 coppie/reserve: reserve ≤ 1 000 Nm/start e **clipping = 0 su ogni start** (il contatore misura il clipping effettivo dell'azione grezza; più severo del C4 ≤ 30 di F2R per decisione rev2; |a| ≤ 1 ne segue, il massimo grezzo è registrato); **D6 penetrazione ≤ 0,020 m e nessuna terminazione `grf_penetration`** (guardie invariate). Deriva clock (dove valutata): stimatore **unwrapped**, banda periodi [1,45, 1,70] s, ≤ 0,055 ciclo/s. Aggregazione per start, mediana sui cicli validi, start senza cicli = FAIL. PASS → V4; FAIL → prosegue al più il budget DAgger di V2, altrimenti STOP.
- **V4 — Sweep σ {0,0025, 0,005, 0,01}** (0,005 **senza alcuno status privilegiato**: un candidato come gli altri): tre varianti a sola testa σ costante dello stesso student promosso (mean-network bit-identica, derivazione content-addressed); per σ: 6 rollout stocastici (3 start esatti × seed 123/124). **Ogni candidato ripete l'intera batteria di qualità, confrontata con la baseline deterministica V3 dello stesso student:** robustezza (orizzonte 500/500 in ≥ 5/6, ≥ 1 ciclo in ≥ 5/6, nessuna terminazione per penetrazione 6/6); **quattro contatori v3 puliti in ogni run** (HS cancellate 0, resync ≤ 1, timeout stance/swing 0, fallimenti del contratto causale 0); cinematica in **non-inferiorità vs V3** (mediana per start sui seed con cicli validi): knee RMSE ≤ V3_start + 0,02 rad e r ≥ V3_start − 0,03, ankle RMSE ≤ V3_start + 0,03 rad, **min caviglia ≤ −0,03 mantenuto (2/3 start)**, stance ∈ [0,50, 0,75] (2/3); coppie/clipping/penetrazione: **clipping = 0 in ogni run**, penetrazione ≤ 20 mm, reserve ≤ 1 000 Nm. **Selezione congelata (mai "largest passing"):** (1) elimina i σ che falliscono qualunque elemento; (2) fra i superstiti ordina per **robustezza** (numero dei 6 run con orizzonte e ≥ 1 ciclo); (3) a parità, per **qualità** (vettore delle mediane knee RMSE, −r, ankle RMSE, penetrazione, reserve); (4) **il σ maggiore solo come tie-break finale**, quando robustezza pari **e** ogni metrica di qualità entro i margini di equivalenza preregistrati (knee RMSE 0,01; r 0,02; ankle RMSE 0,02; penetrazione 1 mm; reserve 50 Nm). Nessuno sguardo al seed 125 né a ricomputi del gate per la selezione; nessun σ supera → FAIL di stadio, STOP. Dopo la selezione: **gate held-out seed 125** (3 stocastici al σ scelto: orizzonte 3/3, ≥ 1 ciclo/start, nessuna terminazione per penetrazione, C1–C3); un FAIL blocca la promozione. Seed 126–128 sigillati.
- **V5 — Critic warmup** (ricetta 13/07): PPO con **actor bit-congelato** (digest identico prima/dopo, verificato), 1 iterazione preregistrata (estensione max 5 solo per decisione esplicita pre-lancio), al σ selezionato; export **H0_V26B** con l'intera batteria di invarianti. STOP: qualsiasi fine-tuning PPO dell'attore è un protocollo separato (evidenza di luglio: H1 e pilot50 fallirono i gate fisici da JUL_H0).

## Regole trasversali
Seed: 123 det; 123/124 raccolta; **125 held-out di promozione** (mai in dataset/etichette/fit/selezione); 126–128 sigillati. Artefatti: no-clobber/content-addressed, prenotazioni esclusive + fill atomico, export transazionale con lock e rename nativo no-replace, receipt con chiusura F0 e preflight scoping. Immutabili: F0/F1/F2R, i 3 file dirty user-owned, produzione/reward/corridoio/guardie, i checkpoint di controllo. Questo documento **non autorizza** alcuna esecuzione.
