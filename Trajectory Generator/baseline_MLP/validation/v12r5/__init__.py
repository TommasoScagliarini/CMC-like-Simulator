"""Additive V12R5 case-balanced recovery lineage.

Importing this package only registers its local validation path in
``sys.path``.  Protocol publication and runtime execution remain separate,
explicit operations.
"""
